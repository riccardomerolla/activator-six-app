GET     /samples/:guid     controllers.Samples.getGuid(guid: String)

#-----------------------------------
# Custom routes                    |
#-----------------------------------
GET     /                           controllers.Application.index()
